import React from 'react';
import {Typography} from 'antd'

const {Title} = Typography

const PageTitle = () => {
    return (
        <div>
            <Title style={{color:'#808080', marginBottom:10}}>Dashboard</Title>
        </div>
    );
}

export default PageTitle;
